from setuptools import setup, find_packages
# This is for the purposes of running unit tests and does not normally need to
# be run
setup(name='Data Quality Tester', version='0', packages=find_packages())
